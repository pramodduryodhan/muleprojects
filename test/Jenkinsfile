pipeline {
  agent any
  stages {
    
    stage('Build') { 
      steps {
			withMaven(maven:'maven') {
				sh 'mvn -f  test/pom.xml clean install'
			}
        
      }
    }
    
  }
}